import styled, { css } from 'styled-components';

export const Container = styled.a`
  ${() => css`
    display: block;
    position: relative;
    color: inherit;
    width: 180px;
    height: 3rem;
  `}
`;
